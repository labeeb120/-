# تقرير تحليل مشروع معهد الخنساء (Visual Basic .NET)

## 1. وصف المشروع
المشروع هو نظام إدارة لمعهد تعليمي يسمى "معهد الخنساء". تم تطويره باستخدام لغة **Visual Basic .NET** ويستهدف منصة **.NET Framework 4.7.2**. يعتمد النظام على واجهات المستخدم الرسومية (Windows Forms) لإدارة العمليات اليومية للمعهد.

## 2. التقنيات المستخدمة
*   **اللغة البرمجية:** VB.NET.
*   **قاعدة البيانات:** Microsoft Access (`users.accdb`).
*   **نمط التصميم:** فصل منطق البيانات في ملف مساعد (`DataAccessHelper.vb`).
*   **المكتبات:** استخدام مكتبات `System.Data.OleDb` للتعامل مع قاعدة البيانات.

## 3. المكونات الرئيسية (الواجهات)
*   **`Form1.vb`:** شاشة تسجيل الدخول.
*   **`mane.vb`:** الواجهة الرئيسية لإدارة الإيرادات والتحصيل المالي.
*   **`studnt.vb`:** شاشة إدارة بيانات الطلاب.
*   **`emp.vb`:** شاشة إدارة الموظفين.
*   **`Expenses.vb`:** شاشة متابعة المصروفات.
*   **`Receipt.vb`:** شاشة إصدار السندات والتقارير.

## 4. ما الذي ينقصه المشروع (النواقص)
1.  **الأكواد الوظيفية:** معظم الواجهات تحتوي على أزرار مصممة ولكن بدون أكواد برمجية (مثلاً أزرار الحفظ والحذف في واجهة الطلاب والموظفين لا تقوم بأي عمل حالياً).
2.  **إعدادات الاتصال:** مسار قاعدة البيانات في `GlobalVariables.vb` يحتاج للتعديل ليكون ديناميكياً ليفتح على أي جهاز.
3.  **الربط المنطقي:** التنقل بين الشاشات (مثلاً من شاشة الدخول إلى الشاشة الرئيسية) غير مكتمل برمجياً.
4.  **التحقق من البيانات:** يفتقر النظام لأكواد التحقق من صحة البيانات المدخلة قبل حفظها.

## 5. الخطة المقترحة لتشغيل وتطوير المشروع
1.  **إصلاح الاتصال:** تعديل Connection String في `GlobalVariables.vb` ليستخدم `Application.StartupPath`.
2.  **برمجة شاشة الدخول:** تفعيل التحقق من اسم المستخدم وكلمة المرور من قاعدة البيانات.
3.  **تفعيل العمليات الأساسية:** كتابة أكواد الإضافة والتعديل والحذف لكل واجهة باستخدام `DataAccessHelper`.
4.  **نظام التقارير:** تفعيل أزرار الطباعة والتصدير (Excel/PDF) الموجودة في واجهة الإيرادات.
5.  **الاختبار:** إجراء فحص شامل للتأكد من حفظ البيانات واسترجاعها بشكل صحيح.
